---
- name: 'Prep'
  ansible.builtin.import_tasks: 'prep.yml'

- name: 'Backup'
  when: navidrome_flg_backup
  ansible.builtin.import_tasks: 'backup.yml'

- name: 'Restore'
  when: navidrome_flg_restore
  ansible.builtin.import_tasks: 'restore.yml'

- name: 'Maintenance'
  when: navidrome_flg_maintenance
  ansible.builtin.import_tasks: 'maintenance.yml'

- name: 'Install'
  when: navidrome_flg_install
  ansible.builtin.import_tasks: 'install.yml'

- name: 'Config'
  when: navidrome_flg_config
  ansible.builtin.import_tasks: 'config.yml'

- name: 'Services'
  when: navidrome_flg_install or navidrome_flg_config
  ansible.builtin.import_tasks: 'services.yml'

- name: 'Start'
  ansible.builtin.import_tasks: 'start.yml'
